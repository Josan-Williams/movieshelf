# MovieShelf – AI Assistance Log

Record every meaningful use of AI tools here while the details are fresh. Only record real interactions and changes you actually made.

## Tools and usage policy

- **AI tools used:** Perplexity Computer (chat – mentoring, design review, code review, debugging guidance) and ChatGPT (chatgpt.com – schema suggestions).
- **How AI is used:** AI assistance on this project is via chat only. All code is written and reviewed by me.
- **Editor AI:** GitHub Copilot, GitHub Copilot Chat and Blackbox are disabled for this workspace. `AGENTS.md` and `CLAUDE.md` (instruction files for AI coding agents, generated by create-next-app) were removed because no AI agent edits code in this repository.
- **Secrets:** No API keys, credentials or personal data are shared with AI tools.

## Log

| # | Date | AI tool | Task | AI suggestion | What I did (accepted / corrected / rejected) | Why | How I verified |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-23 | ChatGPT (chatgpt.com) | Database schema review | Named collections: `collections` + `collection_movies` tables | **Rejected** – replaced with one personal collection per user (`collection_items`) | Not required by the spec; adds IDOR risk and extra ownership checks, constraints, UI and tests | Compared against the spec wording; see DEC-001. Tests to add: duplicate add, removal, cross-user access |
| 2 | 2026-09-23 | ChatGPT (chatgpt.com) | Database schema | `movies` table without a TMDB identifier | **Corrected** – added `tmdb_id UNIQUE NOT NULL` | Without it the same film could be stored twice and couldn't be linked back to TMDB | Duplicate `tmdb_id` insert rejected by `uq_movies_tmdb_id` in a scratch Postgres run |
| 3 | 2026-09-23 | Perplexity Computer | Database schema | Single nullable `director` text column on `movie` | **Corrected** – replaced with `directors` + `movie_directors` tables keyed by TMDB person ID | Loses co-directors; director names aren't unique | Scratch Postgres run of combined schema |
| 4 | 2026-09-24 | Perplexity Computer | Local database setup | Generated `docker-compose.yml` (dev + test Postgres 18.6 services) and `.env.example` at my explicit request, after I chose the two-container design (DEC-003) | **Modified** – (1) renamed the volume `movieshelf_pgdata` → `pgdata` to avoid Compose's doubled name `movieshelf_movieshelf_pgdata`, and removed the orphaned old volume by hand; (2) bound both ports to `127.0.0.1` because the generated `"5432:5432"` mapping listened on `0.0.0.0`, exposing the databases to the local network | Tidier resource naming; security – local databases should not be reachable from shared Wi-Fi | `docker compose ps` → both services healthy on `127.0.0.1:5432` / `127.0.0.1:5433`; `docker volume ls` → only `movieshelf_pgdata`; restart test → table in `db_test` gone after `down`/`up` (tmpfs works); dev-volume persistence check → `persist_check` row (id = 1) survived `down`/`up` in `db`; `psql` confirmed PostgreSQL 18.6 |

Rows 1–3 confirmed by the developer on 2026-09-23 as reflecting actual decisions. Row 4 reviewed and verified by the developer on 2026-09-24.
